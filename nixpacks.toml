[phases.setup]
nixPkgs = [
  "python311",
  "ffmpeg",
  "libGL",
  "libGLU",
  "fontconfig",
  "dejavu_fonts",
  "freetype",
  "libjpeg",
  "zlib",
]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 300 --worker-class sync --max-requests 1000 --preload"
