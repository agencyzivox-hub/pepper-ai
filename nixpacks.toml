[phases.setup]
nixPkgs = [
  "python311",
  "python311Packages.pip",
  "python311Packages.setuptools",
  "python311Packages.wheel",
  "ffmpeg",
  "libGL",
  "libGLU",
  "fontconfig",
  "dejavu_fonts",
  "freetype",
  "libjpeg",
  "zlib"
]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 300 --preload"